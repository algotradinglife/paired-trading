# PA Feitian Read-Only Dashboard

Static dashboard shell for `src/tests/fixtures/pa_feitian_snapshot_v0.json`.

Run from this directory:

```bash
npm run smoke
npm run serve
```

The dashboard fetches the shared snapshot fixture contract and does not read raw
data stores or strategy internals.
